<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Post</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 antialiased min-h-screen">
    <!-- Create Post Section -->
<section class="bg-gray-900 text-gray-100 py-16 min-h-screen">
    
  <header class="bg-gray-900 text-gray-100 p-4">
    <div class="max-w-6xl mx-auto flex items-center justify-between">
      
      <!-- Left: Logo or site name -->
      <div class="text-xl font-bold">
        <!-- Dark Blog -->
      </div>
  
      <!-- Right: Profile Button -->
      <div>
        <a href="/profile" class="bg-red-400 hover:bg-blue-500 text-white px-4 py-2 rounded-lg transition">
          Profile
        </a>
      </div>
  
    </div>
  </header>
    <div class="max-w-3xl mx-auto px-6">
      <h1 class="text-3xl sm:text-4xl font-bold mb-6 text-center">
        Create a New Post
      </h1>
      <p class="text-gray-400 mb-10 text-center">
        Share your thoughts with the community. Fill in the details below.
      </p>
  
      <form action="/newpost" method="POST" class="space-y-6 bg-gray-800 p-8 rounded-2xl shadow-xl">
        
        <!-- Title -->
        <div>
          <label for="title" class="block text-sm font-medium mb-2">Title</label>
          <input 
            type="text" 
            name="title" 
            id="title" 
            placeholder="Post title..." 
            required
            class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>
  
        <!-- Content -->
        <div>
          <label for="content" class="block text-sm font-medium mb-2">Content</label>
          <textarea 
            name="content" 
            id="content" 
            rows="6" 
            placeholder="Write your post here..." 
            required
            class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
          ></textarea>
        </div>
  
        <!-- Tags -->
        <div>
          <label for="tags" class="block text-sm font-medium mb-2">Tags (comma separated)</label>
          <input 
            type="text" 
            name="tags" 
            id="tags" 
            placeholder="e.g., Universe, Star, Nebula"
            class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>
  
        <!-- Submit Button -->
        <div class="text-center">
          <button 
            type="submit"
            class="px-6 py-3 bg-blue-600 hover:bg-blue-500 rounded-lg font-semibold transition"
          >
            Publish Post
          </button>
        </div>
  
      </form>
    </div>
  </section>
  
</body>
</html>







